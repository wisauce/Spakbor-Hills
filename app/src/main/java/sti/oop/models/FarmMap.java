package sti.oop.models;

import javafx.scene.layout.StackPane;

public class FarmMap extends StackPane {
  //nanti ini buat nyimpen gridnya

  // public final Canvas canvas;
  // public final GraphicsContext gc;
  // public final int screenWidth = App.screenWidth;
  // public final int screenHeight = App.screenHeight;

  // public FarmMap() {
  //   super();
  //   canvas = new Canvas(screenWidth, screenHeight);
  //   gc = canvas.getGraphicsContext2D();
  //   this.getChildren().add(canvas);
  // }

  // public void render(Stage stage) {
  //   gc.clearRect(0, 0, screenWidth , screenHeight);
  //   gc.fillRect(x, y, tileSize , tileSize); // Clear screen
  //   gc.drawImage(image, x, y);
  // }
}
